{
  "name": "ddd-web-api",
  "private": true,
  "description": "Tooling for the ddd-web-api demo repository (Markdown linting only for now; app-level tooling lives in service-api/, bff/, and frontend/ once scaffolded).",
  "scripts": {
    "lint:md": "remark . --frail --quiet"
  },
  "devDependencies": {
    "remark-cli": "^12.0.1",
    "remark-lint-maximum-line-length": "^4.1.1",
    "remark-preset-lint-consistent": "^6.0.1",
    "remark-preset-lint-recommended": "^7.0.1"
  }
}
